/** Initializes a new smiley circular motion and starts running it. */
class Main {
    function void main() {
        var GridGame game;
        let game = GridGame.new();
        do game.run();
        do game.dispose();
        return;
    }
}